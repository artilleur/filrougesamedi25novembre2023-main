{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Date_commande</th>
                <td>{{ commande.dateCommande ? commande.dateCommande|date('Y-m-d') : '' }}</td>
            </tr>
            {# <tr>
              <th>prix</th>
            <td>{{ commande.prix }}</td>
            </tr> #}
            <tr>
                <th>Adresse</th>
                <td>{{ commande.adresse }}</td>
            </tr>
            <tr>
                <th>Adresse_fact</th>
                <td>{{ commande.adresseFact }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commande.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_crud_index') }}">back to list</a>

    {# <a href="{{ path('app_commande_crud_edit', {'id': commande.id}) }}">edit</a> #}

    {{ include('commande_crud/_delete_form.html.twig') }}
{% endblock %}
